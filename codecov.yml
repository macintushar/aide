coverage:
  status:
    project: true
    patch:
      default:
        target: 85%
        threshold: 0%

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  behavior: default
